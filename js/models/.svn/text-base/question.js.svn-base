define([
  'Underscore',
  'Backbone'
], function(_, Backbone) {
  var questionModel = Backbone.Model.extend({
    defaults: {
      score: 10
    },
    initialize: function(){
    }
    
  });
  return questionModel;

});
